pre-commit:
  jobs:
    # Oxfmt only: oxlint has a large backlog; full `pnpm fix` blocks commits.
    # Run `pnpm check` locally or rely on CI before merging.
    - run: pnpm exec oxfmt .
      glob:
        - "*.js"
        - "*.jsx"
        - "*.ts"
        - "*.tsx"
        - "*.json"
        - "*.jsonc"
        - "*.css"
      stage_fixed: true
